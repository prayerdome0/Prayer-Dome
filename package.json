{
  "name": "prayer-dome",
  "version": "1.0.0",
  "private": true,
  "description": "Prayer Dome web app and Android package",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "mobile:prepare": "node scripts/prepare-mobile.mjs",
    "mobile:sync": "npm run mobile:prepare && npx cap sync android",
    "mobile:open": "npm run mobile:sync && npx cap open android",
    "mobile:run": "npm run mobile:sync && npx cap run android",
    "mobile:build:debug": "npm run mobile:sync && cd android && ./gradlew assembleDebug",
    "mobile:build:release": "npm run mobile:sync && cd android && ./gradlew bundleRelease assembleRelease",
    "test": "node tests/assistant.test.js && node tests/content.test.js && node tests/academy.test.js && node tests/admin.test.js && node tests/certificate.test.js && node tests/pages.test.js && node tests/premium.test.js && node tests/challenge.test.js && node tests/certificates.test.js",
    "lint": "node scripts/validate-production.mjs && python3 seo/apply-seo.py --check",
    "functions:verify": "npm --prefix functions run verify",
    "build": "npm run lint && npm test && npm run functions:verify"
  },
  "dependencies": {
    "@capacitor/android": "8.5.0",
    "@capacitor/core": "8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.0",
    "jsdom": "^26.1.0"
  }
}
